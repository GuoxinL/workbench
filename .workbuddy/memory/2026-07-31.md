# 2026-07-31

## 测试个人工作台部署与 GitHub 同步
- 项目：纯静态待办+双链笔记 PWA（GitHub Pages 部署代码，数据走独立私有仓库 Contents API）。
- 测试目标：验证 Pages 站点在线 + 数据同步引擎（token/repo/读写回环）是否正常。
- 结果：全部通过。
  - Pages `https://guoxinl.github.io/workbench/` 返回 200，版本 `20260730-2348`，标题「个人工作台」，全部静态资源(manifest/css/js/icon) 200。
  - 细粒度 token 有效（login=GuoxinL），仓库 `GuoxinL/workbench-data` 私有、默认分支 `main`。
  - 数据文件 `data/workbench.json` 可读：含 version/updatedAt/todos(4)/notes(3)。
  - 写回环测试：写入测试探针 → 重读确认存在 → 还原为原文(sha 与原始一致)，探针已清除。未污染用户数据。
- 测试方法：Python(urllib)+cURL 直连 api.github.com，token 经环境变量传入，未落盘明文。
- 注：token 为细粒度令牌（github_pat_...），仅授权 workbench-data 仓库 Contents 读写，符合最小权限。
