# 04. UT —— 单元测试

> 与 03 合并实现窗口：23:19:11 → 23:44:23（非小需求模式，但 03/04 产物同批产出）

## 一、用例清单（与 Plan §九 对齐）

### 新增单测
| 文件 | 用例 | 覆盖 |
|------|------|------|
| `blobSha.test.ts` | 空内容 = git 已知空 blob sha `e69de29…` | 公式锚定（与 GitHub 一致） |
|  | 与 Node crypto 参考实现逐字节一致（含多字节/长串） | 编码与 header 正确 |
|  | 内容变化即不同 sha | 「是否变化」判定基石 |
| `listDir.test.ts` | `listDir` 投影 `{path,sha}`、忽略子目录（images/） | 索引只列文件 |
|  | 目录 404 → 空数组（首次同步） | 空仓库兜底 |
|  | `fetchIndex` 现拉 `kb/`+`todos/` → `id→sha`，不触碰正文 | 替代 manifest.json |
| `diff.test.ts` | A 本地无/远端有→pull | 矩阵 A |
|  | B 本地有/远端无→push | 矩阵 B |
|  | C 两边存活 & sha 一致→skip | 矩阵 C（用户要的「sha 一致不更新」） |
|  | D 本地未动/远端改→pull | 矩阵 D |
|  | E 远端未动/本地改→push | 矩阵 E |
|  | F 双方都改→conflicts | 矩阵 F |
|  | G 本地软删/远端有→del | 矩阵 G |
|  | 首轮 base 缺失：localSha≠tree→conflicts；localSha===tree→skip | 迁移自愈边界 |
|  | 本地软删但远端已无→无动作 | 墓碑 TTL 边界 |

### 改写单测
| 文件 | 关键改动 |
|------|----------|
| `contents.test.ts` | `fetchManifest`→`fetchIndex`；`pushRemote` 新签名（`pushIds/treeShaByPath/delIds`）；不再写 `manifest.json`（断言 `shaByPath[path]` 替代 `manifest.articles`）；新增 DELETE / 409 conflictSlug / serialize 一致性用例 |
| `engine.test.ts` | 新 `ContentsApi`（`fetchIndex`/`fetchArticles(ids,config)`/`fetchTodos(ids,config)`/`pushRemote` 新返回）；新 `SyncAdapter`（`getSyncState/setSyncState/purgeLocal`，去 `manifestSha`）；`uptodate` 短路断言；墓碑 DELETE + `purgeLocal` 断言；冲突重试 / 并发排队 / 最大重试仍保留 |

### 未改动
- `manifest.test.ts`：`getManifest`/`planDiff`/`entryOf` 仍导出，`@deprecated` 标记，测试原样通过。

## 二、覆盖率与未覆盖行
- 核心逻辑（blobSha / listDir / diff / engine 主路径 / contents.pushRemote）均有针对性用例，矩阵 A–G 全覆盖。
- 未覆盖：真实网络失败分支（401/403/429/超时）走 `client.ts` 的既有单测；`publishToMirror`/`pushImage` 沿用旧实现未新增用例（非本次改动核心）。
- `engine.ts` 的 `schedulePush`/`startPolling` 计时逻辑由既有 S3/S4 行为保证，未新增定时器单测（避免 flaky）。

## 三、结果
- `npm test`：34 文件 / 201 用例全绿。
- `npm run build`：类型检查零错误、产物构建成功。
